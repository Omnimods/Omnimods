if not omni then omni = {} end
if not omni.fluid then omni.fluid={} end

omni.fluid.sluid_contain_fluid = 40
omni.fluid.sluid_stack_size = 360

require("prototypes.functions")
require("prototypes.sluids-boiler")